using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public enum Element //Vari elemeti per l'arma (Weapon) e per le debolezze o resistenze (Hero)
{
    None = 0,
    Fire = 1,
    Ice = 2,
    Lightning = 3,
}
